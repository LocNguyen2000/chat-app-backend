export interface userInterface {
    id: Number,
    name: string,
    email: string,
    password: string
};

export interface registerInterface {
    name: string,
    email: string,
    password: string
};

export interface loginInterface {
    email: string,
    password: string
};
