export interface userInterface {
    id: Number,
    name: string,
    email: string,
    password: string
};

export interface registerInfo {
    name: string,
    email: string,
    password: string
};

export interface loginInfo {
    email: string,
    password: string
};
